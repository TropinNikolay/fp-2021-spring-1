module Main where

-- Функция getArgs позволяет получить список аргументов,
-- с которыми был запущен хаскельный скрипт
--
import System.Environment (getArgs)
-- Функция getLine получает из хэндла ровно одну строку.
-- Обратите внимание, что следующий вызов getLine на хэндле выдаст следующую строку из него.
--
import System.IO (getLine)

main :: IO ()
main = undefined